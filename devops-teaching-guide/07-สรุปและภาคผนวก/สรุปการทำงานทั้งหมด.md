# สรุปการทำงานทั้งหมด (จาก Commit ถึง Production)

1. นักพัฒนาส่งโค้ดขึ้น GitLab (main/dev branches)
2. GitLab CI เริ่ม pipeline: build → test → (ถ้า main) deploy
3. บนเซิร์ฟเวอร์รัน Docker Compose เปิด backend และฐานข้อมูล
4. Nginx reverse proxy รับทราฟฟิกเว็บจริง
5. Monitoring & Logging ติดตามสุขภาพระบบและวิเคราะห์ปัญหาได้
